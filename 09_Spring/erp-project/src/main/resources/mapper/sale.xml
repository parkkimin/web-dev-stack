
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.erp.mapper.QualityMapper">
<insert id="addSale" parameterType="Sale">
INSERT INTO sale(sale_date, product_name, category, gender,)
VALUES(#{saleDate}, #{productName})
</insert>
<select id="viewSale">
SELECT * FROM sale
</select>
</mapper>

private int;
private LocalDate saleDate;
private String;
private String;
private String;
private 
private
private